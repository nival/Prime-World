if platform == 'win32':
  if configuration == 'debug':
    includePaths = [ 
      'include'
      ]
  else:
    includePaths = [ 
      'include'
      ]
else:
  includePaths = []

if platform == 'win32':
  if configuration == 'debug':
    libDependencies = [
      'comsuppwd.lib',
      'Winmm.lib',
      'gtrtst32.lib', 
      'quartz.lib',
      'strmbasd.lib',
      'strmiids.lib'
      ]
  else:
  	libDependencies = [
      'comsuppw.lib',
      'Winmm.lib',
      'gtrtst32.lib', 
      'quartz.lib',
      'strmbase.lib',
      'strmiids.lib'
      ]
else:
  libDependencies = []

libPaths = [
    'lib'
]




