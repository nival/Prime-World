// -*- MPC -*-
// $Id: mpb.mpb 1190 2007-08-03 11:52:59Z elliott_c $

project {
  // Allow users to specify .mpb files in their project.  This gives the
  // opportunity to see them in IDE's and to install them using
  // prj_install.pl.
  Define_Custom(MPB) {
    automatic = 0
    inputext = .mpb
  }
}
