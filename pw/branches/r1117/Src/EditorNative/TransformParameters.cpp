#include "stdafx.h"
#include "TransformParameters.h"

using namespace EditorNative::Manipulators;

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
TransformParameters::TransformParameters( float _minValue, float _maxValue )
: isEnabled( _minValue < _maxValue ),
	minValue( _minValue ),
	maxValue( _maxValue )
{
}
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
