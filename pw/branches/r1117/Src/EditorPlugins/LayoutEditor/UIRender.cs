using System;
using System.Drawing;
using EditorLib.UIScene;

namespace EditorPlugins.LayoutEditor
{
	/*internal class UIRender
	{
		private UISceneControl scene = null;

		public UIRender( UISceneControl scene )
		{
			this.scene = scene;
		}

		public virtual void DrawWindow( Window window )
		{
			// here we do nothing, cause UISceneControl draw entire layout itself
		}

		public virtual void DrawPoint( int x, int y, int size, Color color )
		{
			scene.DrawPoint( x, y, size, color );
		}

		public virtual void DrawLine( int x1, int y1, int x2, int y2, Color color )
		{
			scene.DrawLine( x1, y1, x2, y2, color );
		}

	}*/
}
