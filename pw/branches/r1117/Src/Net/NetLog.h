#pragma once
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
NLogg::CLoggerStream &GetNetLog();
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#define netTrace ( NLogg::StreamLocker( GetNetLog() ) ).GetLog().SetFileAndLevel( NLogg::LEVEL_DEBUG, __FILE__, __LINE__ ) << GetNetLog().GetName() << ": " .Trace

#define netTraffic( level, msg )\
{\
	GetNetLog().GetMutex().Lock();\
	GetNetLog().SetFileLine( __FILE__, __LINE__ );\
	GetNetLog().SetLevel( level );\
	GetNetLog() << GetNetLog().GetName() << ": ";\
	GetNetLog().Trace( msg );\
  GetNetLog().GetMutex().Unlock();\
}

//#define netTraffic( level, msg )

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
