sources = [ 
  'UserNotificationIface.h',
  'UserInfo.cpp',
  'UserInfo.h',
]

includePaths = [
  '..'
]

components = [ 
  'Vendor/Libc',
  'RPC/all',
  'ClusterTypes',
]

platformFeatures = { 'win32' : Win32Features( 'stdafx.h' ) }
