// Automatically generated file, don't change it manually!
#include "stdafx.h"
#include "../libdb/Checksum.h"
#include "../libdb/XmlSaver.h"
#include "../Scripts/lua.hpp"
#include "../Scripts/ScriptMacroses.h"
#include "../Scripts/TypeWrapper.h"

#include "../UI/DBUI.h"
#include "../Render/DBRender.h"
#include "DBUIRoot.h"
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
REGISTER_DBRESOURCE( H6UIRoot );
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

namespace NDb
{
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
UICursorDesc::UICursorDesc()
{
}
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
int UICursorDesc::operator&( IBinSaver &saver )
{
	saver.Add( 2, &name );
	saver.Add( 3, &cursor );
	saver.Add( 4, &cursorOverlay );
	saver.Add( 5, &cursorOverlayOffset );

	return 0;
}
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
int UICursorDesc::operator&( IXmlSaver &saver )
{
	saver.Add( "name", &name );
	saver.Add( "cursor", &cursor );
	saver.Add( "cursorOverlay", &cursorOverlay );
	saver.Add( "cursorOverlayOffset", &cursorOverlayOffset );

	return 0;
}
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
UISceneEnvironment::UISceneEnvironment()
{
}
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
int UISceneEnvironment::operator&( IBinSaver &saver )
{
	saver.Add( 2, &ID );
	saver.Add( 3, &lightEnvironment );

	return 0;
}
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
int UISceneEnvironment::operator&( IXmlSaver &saver )
{
	saver.Add( "ID", &ID );
	saver.Add( "lightEnvironment", &lightEnvironment );

	return 0;
}
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
UISoundDesc::UISoundDesc()
{
}
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
int UISoundDesc::operator&( IBinSaver &saver )
{
	saver.Add( 2, &name );
	saver.Add( 3, &soundFileName );

	return 0;
}
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
int UISoundDesc::operator&( IXmlSaver &saver )
{
	saver.Add( "name", &name );
	saver.Add( "soundFileName", &soundFileName );

	return 0;
}
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
H6UIRoot::H6UIRoot()
{
}
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
int H6UIRoot::operator&( IBinSaver &saver )
{
	saver.Add( 2, &cursors );
	saver.Add( 3, &sounds );
	saver.Add( 4, &sceneEnviroments );

	return 0;
}
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
int H6UIRoot::operator&( IXmlSaver &saver )
{
	saver.Add( "cursors", &cursors );
	saver.Add( "sounds", &sounds );
	saver.Add( "sceneEnviroments", &sceneEnviroments );

	return 0;
}
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
bool H6UIRoot::GetVariantValue( NScript::VariantValue & value, const char *key ) const
{
	return false;
}
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
}; // namespace NDb
