#pragma once

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
namespace NLogg
{
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class LogMover
{
public:
  static LogMover & GetInstance(); // singleton
  bool Init(int argc, char* argv[]); // разбираем свои опции командной строки, составляем список файлов, годных для переноса; return = есть ли в списке хоть один файл
  bool Move( const nstl::string & _substr = "" ); // выполняем перенос файлов (опционально - содержащих в названии подстроку <_substr>); return = переместили ли хоть один

protected:
  LogMover() {}

  vector<string> oldLogFileNames;
  string moveLogsTo;
	float oldLogDaysLimit;
};

} // namespace NLogg
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////



