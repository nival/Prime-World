#pragma once
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
namespace Render
{
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
enum EMultiSampleType 
{
	MULTISAMPLE_NONE = 0,
};
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Параметры режима рендеринга
struct RenderMode
{
	EMultiSampleType multiSampleType;
	unsigned int multiSampleQuality;
	bool isFullScreen;
  bool isBorderless;
	unsigned int width;
	unsigned int height;
  unsigned int width3D;
  unsigned int height3D;
	unsigned int refreshRate;
	unsigned int vsyncCount;

	RenderMode();
	
	static const unsigned int WIDTH_DEFAULT;
	static const unsigned int HEIGHT_DEFAULT;
	static const unsigned int REFRESH_RATE_DEFAULT;
};
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

void GetRenderModeFromConfig(RenderMode &renderMode);
void GetResolution( unsigned int &width, unsigned int &height );
void SetResolution( unsigned int width, unsigned int height );
void GetDesktopResolution( unsigned int &width, unsigned int &height );
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
}; // namespace Render