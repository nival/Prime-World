filter = "LocationServer/VoxelGrid"

sources = {
  '' : 
        [ 
          'VoxelGrid.h', 
          'VoxelGrid.cpp', 
        ]
}

# platformFeatures = { 'win32' : Win32Features( 'stdafx.h' ) }

components = [
    'Vendor/Libc',
    'System/Storage',
]
