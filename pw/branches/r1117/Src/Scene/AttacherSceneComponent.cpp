#include "stdafx.h"
#include "AttacherSceneComponent.h"

namespace NScene
{

/*
void AttacherSceneComponent::Update( IScene *pScene, const Placement& parentPos, Render::AABB &objectAABB, float timeDiff )
{
	// Update world placement
	ConstructWorldPlacement(parentPos);

	pObj->SetPlacement(worldPlacement);
	pObj->UpdateForced(timeDiff);
	
	// Copy world AABB
	worldAABB = pObj->GetWorldAABB();
	objectAABB.Grow(worldAABB);
}
*/

}

