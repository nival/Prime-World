#include "stdafx.h"
#include "Network.h"
namespace Network
{
  //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
  bool IsConnectionClosed( IConnection* pConnection )
  {
    return ( pConnection == NULL ) || pConnection->GetStatus() == ConnectionState::Closed;
  }
  //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
}

// пока вроде не требовались другие forward-refcounts
NI_DEFINE_REFCOUNT( Network::IConnection );
