package classes.ContMenu 
{
	import BaseClasses.BaseContextItem;
		
	public class ContextItemFollow extends BaseContextItem
	{
		override public function Press():void
		{
//			FSCommands.Send(FSCommands.FOLLOW_UNIT, int(isOurHero) + " " + heroID);
		}
	}

}