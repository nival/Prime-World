package ContMenu 
{
	import BaseClasses.BaseContextItem;
	
	public class ContextItemWatch extends BaseContextItem
	{
		override public function Press():void 
		{
		//	FSCommands.Send(FSCommands.WATCH_UNIT, int(isSelection) + " " + heroID);
		}
		
	}

}